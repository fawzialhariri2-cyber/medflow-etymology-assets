# MedFlow Workshop Asset Pack — rebuilt

This ZIP is for **hosting the assets**, not for uploading the ZIP itself into Gemini Canvas.

## Next step
1. The assets are already hosted publicly in this GitHub repository.
2. `URL_MANIFEST_TEMPLATE.csv` and `IMAGE_MANIFEST.csv` already contain the direct raw HTTPS URLs.
3. Give Gemini the URL manifest + `CROP_AND_USE_RULES.md`.
4. Paste `GEMINI_IMAGE_INTEGRATION_PROMPT.txt` in the SAME conversation that contains the strong 18-slide Flash+Extended deck.
5. Export to Google Slides, then repair any remaining weak slides one-by-one.

The current Flash+Extended deck is the visual master. Do not request another global redesign.
